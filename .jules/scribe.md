## 2024-05-24 - Python Version Mismatch **Gap:** README.md stated "Python 3.10+" but `pyproject.toml` enforces ">=3.11,<4.0". This could cause installation failures for users following the README. **Fix:** Updated README.md to specify Python 3.11+.
## 2024-05-25 - Risk Engine Safety Precedence **Gap:** The `RiskEngine.process_intents` method enforced a critical hierarchy of safety checks (Kill Switches > Liquidity > Caps > Limits > Dust) but this logic was only implicit in the code structure (200+ lines), making it hard to audit or understand without deep reading. **Fix:** Added a comprehensive docstring to `RiskEngine.process_intents` explicitly detailing the precedence order and behavior of each safety gate.
